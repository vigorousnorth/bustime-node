var apiObj = {
  key: 'F8Z2bctSjsDECRDE6dKsiAZ2V',
  host: 'bustracker.gocarta.org'
}

var bustime = require('./index')(apiObj);

var reqObj = {
  rt: '',
  // dir: '1',
  stpid: '897',
  services: {
    calculateETA: true
  }
}
var fs = require('fs');
bustime.predictions(reqObj, function (err, result) {
  fs.writeFileSync('testObj.js', JSON.stringify(err || result, null, 2));
});
